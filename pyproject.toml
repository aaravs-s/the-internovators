[project]
name = "safewalkers"
version = "0.1.0"
description = "Austin walking route prototype built with FastAPI and Jinja."
requires-python = ">=3.10"
dependencies = [
  "fastapi[standard]>=0.115.0",
]

[tool.ruff]
line-length = 88

